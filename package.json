{
  "name": "couple-menu-bot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local --inspector",
    "test": "cross-env NODE_ENV=test jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nock": "^13.5.0",
    "cross-env": "^7.0.3",
    "node-fetch": "^3.3.2"
  }
}